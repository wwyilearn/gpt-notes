# 标题：

日期：
标签：#chatgpt

## 我的问题

## ChatGPT 回答

## 我的结论

## 后续行动
- [ ]
实验 1：ResNet baseline

只看图片，不看文本，不用 CLIP

作用：
	•	作为基础对照组

⸻

实验 2：CLIP image encoder

把原来的 ResNet backbone 换成 CLIP 的图像编码器

作用：
	•	验证预训练视觉特征是否更有帮助

⸻

实验 3：CLIP + prompt

不仅看图片，还加入文本 prompt

作用：
	•	验证语义提示是否进一步帮助 gaze estimation
【对比试验论文思路】：
为验证预训练视觉语言模型在三维注视估计任务中的有效性，本文首先采用经典卷积神经网络 ResNet18 构建 baseline 模型，在 ETH-XGaze 数据集上建立基础性能参考。随后在此基础上引入 CLIP 图像编码器及提示语义信息，并通过对比实验分析预训练特征与语义提示对注视估计性能的影响。

cat configs/base.yaml
	•	batch size
	•	shuffle
	•	checkpoint
	•	logger
	•	debug 模式
	•	是否快速调试
	•	是否跑 test

所以作者把这些“经常改动的参数”全都拆到 YAML 里。
这样以后你只改参数文件，不用乱改 Python 代码。

现在你还不要正式训练，因为你大概率还没有准备好 ETH-XGaze 数据集路径。
但我们可以先把：
	•	命令形式
	•	配置组合方式
	•	路径写法

本机跑baseline 用500-1000张
服务器 用30%固定子集  数据放在服务器磁盘上
考虑到本地设备主要用于环境调试与流程验证，本文首先在 ETH-XGaze 的小规模子集上完成数据读取、模型训练与日志记录等流程测试；随后在租用服务器环境中进一步扩大训练数据规模，开展正式实验与对比分析。
【进度】申请数据集 申请书件不确定 改下载MPIIFaceGaze
文件路径：~/datasets/MPIIFaceGaze/MPIIFaceGaze
继续下载MPIIGaze 文件路径 /Users/wenya/datasets/MPIIGaze/MPIIGaze
下一步：
跑通预处理脚本 data_processing_mpii.py

找到 sample_root
	2.	改 data_processing_mpii.py
	3.	跑预处理
	4.	看预处理输出长什么样
	5.	再去改项目里的 configs/examples/mpiifacegaze.yaml
	6.	用预处理后的数据跑 baseline
创建预训练脚本输出文件夹mkdir -p ~/datasets/processed/MPIIFaceGaze

测试有没有外网命令 ping -c 4 8.8.8.8



